// implement a recursive depth-first-search algorithm
// that takes a node and returns string
// containing valid HTML for nested unordered lists
//
// WHY? - because this one is a fun one!

module.exports = function treeToHtml(node) {
  var result = "";


  return result;
}
